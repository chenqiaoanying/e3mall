<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.e3mall.search.mapper.CustomItemMapper">
    <select id="selectItemList" resultType="cn.e3mall.common.pojo.SearchItem">
        SELECT
        item.id,
        item.title,
        item.sell_point,
        item.price,
        item.image,
        category.name
        FROM tb_item AS item
        LEFT JOIN tb_item_cat AS category
        ON item.cid = category.id
    </select>
</mapper>